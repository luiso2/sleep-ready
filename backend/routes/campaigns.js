const express = require('express');
const router = express.Router();

// Placeholder - Campaigns routes to be implemented
router.get('/', (req, res) => {
  res.json({
    success: true,
    message: 'Campaigns endpoints coming soon',
    data: []
  });
});

module.exports = router;
